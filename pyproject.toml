[project]
name = "sla-world"
version = "0.1.0"
description = "SLA world-generation and civilization simulation system"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["sla_world*"]
